# Demo Credentials (Development Only)

DEMO DATA ONLY  
DO NOT USE THESE CREDENTIALS IN PRODUCTION

These credentials are intentionally public for local/testing development workflows.

Never reuse them for:

- production platform users
- production tenants
- staging tenants connected to real providers
- real client accounts
- production database users
- production API clients

Default demo password:

- `DemoPassword!2026` (override with `DEMO_USER_PASSWORD`)

Use `php artisan demo:credentials` to print the current seeded accounts.
